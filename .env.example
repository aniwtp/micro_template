# micro_tamplate — example .env configuration
#
# Values are read in order: secrets/ → env → .env (first match wins).
# Uncomment and adjust as needed.

# Database
DB_PATH=test.redb

# Server
BIND_ADDR=localhost:8080
